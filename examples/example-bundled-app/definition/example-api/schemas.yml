Person:
  type: object
  description: A person
  additionalProperties: false
  required: [name]
  properties:
    name: {type: string}
    age: {type: integer, format: int32}
    gender: {type: string, enum: [MALE, FEMALE, UNSPECIFIED]}
    data: {type: string, format: byte}
Greet.Request:
  type: object
  required: [person]
  properties:
    person: {$ref: '#/components/schemas/Person'}
Greet.Response:
  type: object
  required: [message]
  properties:
    message: {type: string, format: byte}
  additionalProperties: false
BarHeader:
  type: string