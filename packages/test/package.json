{
  "name": "test",
  "scripts": {
    "pretest": "npm -w ../lib run build && openapi-ts-backend generate-types api.yml src/gen",
    "test": "LOG_LEVEL=${LOG_LEVEL:=error} jest"
  },
  "devDependencies": {
    "@a-labs-io/openapi-ts-backend-cli": "*",
    "@a-labs-io/openapi-ts-backend": "*"
  }
}